{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "http-server & open http://localhost:8080",
    "lint": "npx prettier . --write && npm run lint.css",
    "lint.css": "npx stylelint '**/*.css' --fix"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "prettier": "3.3.1",
    "stylelint": "^16.6.1",
    "stylelint-order": "^6.0.4"
  }
}
